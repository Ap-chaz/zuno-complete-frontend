// The deployed ZUNO web app that "Get Started" buttons link out to.
// Override via VITE_APP_URL in your .env if the app moves to a new domain.
export const APP_URL = import.meta.env.VITE_APP_URL ?? "https://zuno-fronted-test.pages.dev/";
